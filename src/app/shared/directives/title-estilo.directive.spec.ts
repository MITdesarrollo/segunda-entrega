import { TitleEstiloDirective } from './title-estilo.directive';

describe('TitleEstiloDirective', () => {
  it('should create an instance', () => {
    const directive = new TitleEstiloDirective();
    expect(directive).toBeTruthy();
  });
});
